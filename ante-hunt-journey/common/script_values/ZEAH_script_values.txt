# PoD version is 17 as of 31/08/2026
current_journey_version = 19

eah_hunt_ante_base_progress = 0.3 # 27 years, might be too short but could increase

eah_hunt_ante_progress = {
    add = {
		value = eah_hunt_ante_base_progress
		desc = eah_journey_progress_base
		format = "BASE_VALUE_FORMAT_DECIMALS_MINUS_POSITIVE"
	}
	
	if = {
		limit = { prestige_level >= 1 }
		add = {
			value = eah_hunt_ante_base_progress
			multiply = {
				value = 0.5
				multiply = prestige_level
			}
			desc = eah_journey_progress_prestige
		}
	}
}

eah_hunt_ante_progress_preview = {
    add = {
		value = eah_hunt_ante_base_progress
		desc = eah_journey_progress_base
		format = "BASE_VALUE_FORMAT_DECIMALS_MINUS_POSITIVE"
	}
	
	if = {
		limit = { prestige_level >= 1 }
		add = {
			value = eah_hunt_ante_base_progress
			multiply = {
				value = 0.5
				multiply = prestige_level
			}
			desc = eah_journey_progress_prestige
		}
	}
}