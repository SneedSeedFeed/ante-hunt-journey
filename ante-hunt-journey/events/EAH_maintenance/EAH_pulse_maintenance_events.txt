namespace = EAH_maintenance
# When the inquisition arrives, hunting those dastardly antediluvians is off the table

# close hunts for end game and try to clean up what we can
EAH_maintenance.1 = {
	hidden = yes
	scope = none

	immediate = {
		if = {
			limit = {
				NOT = { global_var:variable_EAH_hunt_closed ?= yes }
				character:POD_wraith_01 ?= { current_year >= inquisition_spawn_year }
			}
			trigger_event = {
				id = EAH_endgame.1
				days = { 0 }
			}
		}
		
		every_player = {
			eah_cleanup_stale_ante_journeys_effect = yes
		}
	}
}